int sensorTop=A0;  // The following corresponds to the infrared sensor mounted in the tank
int sensorBottum=A5;  // The following corresponds to the limit switch On the front of the main body module, This switch is always on switch and on pushing down disconnects the "High" line (5V) From the data input pin
int Relay1=8;   // This is a series relay disguised as one but works in parallel to ensure redundancy alongside dividing the load
int Buzzer=13; // Setup buzzer, An audio indicator For all the actions
int Led=10; // LED indicator light , For visual aid
void setup() 

{ 
  pinMode(sensorTop,    INPUT);
  pinMode(sensorBottum, INPUT);
  
  pinMode(Relay1, OUTPUT);
  pinMode(Buzzer, OUTPUT);
  pinMode(Led,    OUTPUT);

 digitalWrite(Led,HIGH);

  // Simple end-of-cycle melody with increased speed
  // Note durations in milliseconds

  digitalWrite(Buzzer, HIGH);
  delay(50);  // Note 1
  digitalWrite(Buzzer, LOW);
  delay(50);  // Pause
  digitalWrite(Buzzer, HIGH);
  delay(50);  // Note 7
  digitalWrite(Buzzer, LOW);
  delay(50);  // End pause
  digitalWrite(Buzzer, HIGH);
  delay(250);  // Note 7
  digitalWrite(Buzzer, LOW);
 }


void loop() 
{
  
if(digitalRead(sensorBottum)==LOW)
{
   digitalWrite(Led,LOW); 
  digitalWrite(Buzzer,HIGH);
  delay(300);
  digitalWrite(Buzzer,LOW);
  delay(300);
  digitalWrite(Buzzer,HIGH);
  delay(300);
  digitalWrite(Buzzer,LOW);

  digitalWrite(Relay1,HIGH);
 
}
if(digitalRead(sensorTop)==LOW)
{
  digitalWrite(Relay1,LOW);
  digitalWrite(Led,HIGH); 
}
}
